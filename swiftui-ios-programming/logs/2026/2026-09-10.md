---
template_version: "v03"
date: 2026-09-10
book: swiftui-ios-programming
study_day: 13
elapsed_day: 27
duration_minutes: null
status: planned
tags: []
---

# 2026-09-10

## 기본 정보

- 누적 실학습일: 13일
- 시작 후 경과일: 27일차
- 오늘의 목표:
- 총 학습 시간:


## 세션 1

- 시작 시간: `11:32`
- 종료 시간: `11:40`
- 공부 시간: 08분

#### 10.10 지연 저장 프로퍼티

- 프로퍼티를 초기화하는 여러 방법
    1. 직접 할당
    2. 초기화 작업에서 프로퍼티에 값을 할당
    3. 클로저를 이용하여 프로퍼티를 초기화


## 세션 2

- 시작 시간: `12:03`
- 종료 시간: `12:37`
- 공부 시간: 34분

#### 세션 01 이어서(10.10 지연 저장 프로퍼티)
- 프로퍼티를 lazy로 선언하면 프로퍼티가 최초로 접근될 때만 초기화됨. 따라서 리소스를 많이 사용하는 작업은 관련 프로퍼티에 lazy를 사용하여 해당 프로퍼티가 사용될 때까지 리소스 집약적인 활동을 미룰 수 있음. 지연 프로퍼티는 반드시 변수(var)로 선언되어야 함.

책 94쪽 내용
“실행 프로세스의 후반부 단계까지 프로퍼티에 값이 할당되었는지 모르게 되는 상황”
->
“인스턴스를 생성하는 시점에는 그 프로퍼티가 언제 필요할지, 혹은 실제로 사용할지조차 알 수 없는 경우가 있다.”

* lazy var는 프로퍼티의 초기화를 인스턴스 생성 시점이 아니라 해당 프로퍼티가 최초로 사용되는 시점까지 미루는 저장 프로퍼티입니다.

#### 10.11 스위프트에서 self 사용하기
스위프트 프로그래밍 언어는 현재의 클래스 인스턴스에 속한 메서드나 프로퍼티를 가리킬 때를 위한 self 프로퍼티 타입을 제공함.

대부분의 경우 스위프트에서 self는 선택적으로 사용.

* self를 사용해야 하는 상황:
    - 프로퍼티나 메서드를 클로저 표현식 내에서 참조할 경우.
    - 함수의 매개변수가 클래스 프로퍼티와 동일한 이름을 가질 경우

* self 사용 여부는 프로그래머의 취향에 달림. 프로퍼티나 메서드를 참조할 때 self를 사용하기 좋아하는 프로그래머들도 자신이 원하는 방식으로 스위프트 코딩을 계속해 갈 수 있음. 하지만, 스위프트로 프로그래밍할 때 self를 반드시 사용해야 하는 것은 아님.

#### 10.12 스위프트 프로토콜 이해하기

* **프로토콜(protocol)**: 클래스가 충족해야 하는 최소한의 요구사항을 정의하는 규칙들의 집합. 프로토콜은 protocol 키워드를 이용하여 선언되며, 클래스가 반드시 포함해야 하는 메서드와 프로퍼티를 정의함.

```swift
protocol MessageBuilder {

    var name: String {get}
    func buildMessage() -> String
}
```
#### 10.13 불투명 반환 타입

* **불투명 반환 타입(opaque return type)**
    특정 반환 타입(**구체화된 타입(concrete type)**이라고 함)을 지정하는 대신, 불투명 반환 유형을 사용하면 지정된 프로토콜을 따르는 모든 타입이 반환될 수 있게 함.
    불투명 반환 타입은 프로토콜 이름 앞에 some 키워드를 붙여 선언됨.

    공개 API 내에서 사용되는 반환 타입을 숨기기 위해 의도적으로 설계. 구체화된 타입을 감춤으로써 프로그래머는 특정 구체화된 타입을 반환하는 함수에 의존하지 않게 되거나 접근되지 않는 내부 객체에 접근하게 되는 위험이 없어진다.

    - 이해 잘 안됨. 다음 세션에서 이어서.


## 세션 3

- 시작 시간: `15:17`
- 종료 시간: `15:27`
- 공부 시간: 10분

#### 10.13 불투명 반환 타입

- 앞 세션 02에 이어서, 다시 '10.13 불투명 반환 타입'읽으며 이해할 것.

* **불투명 반환 타입(opaque return type)**
    : 특정 반환 타입(**구체화된 타입(concrete type)이라고 함)을 지정하는 대신, 불투명 반환 유형을 사용하면 지정된 프로토콜을 따르는 모든 타입이 반환될 수 있게 함. 불투명 반환 타입은 프로토콜 이름 앞에 some 키워드를 붙여 선언됨.

    불투명 반환 타입은 만든 프로토콜을 포함하여 모든 프로토콜에 사용될 수 있음.

    실제 반환 타입을 아는 게 아니라 반환 타입의 자격에 대해 아는 것.

## 세션 4

- 시작 시간: `16:28`
- 종료 시간: `00:00`
- 공부 시간: 00분

#### 10.13 불투명 반환 타입





## 하루 마무리

- 오늘의 한 줄:
- 오늘의 상태: `완료` / `진행 중` / `막힘`
- 다음 시작점:
